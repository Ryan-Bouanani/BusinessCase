
<div class="modal overlayClose" data-modal>
    <div class="overlay" data-modal-overlay></div>
    <div class="modalContainer">
        <button type="button" class="closeModal" data-modal-close-open><i class="fa-solid fa-rectangle-xmark"></i></button>
        <h2>{{title}}</h2>
        <div class="modalContent">
            {{content}}
        </div>
    </div>
</div>