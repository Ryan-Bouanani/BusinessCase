{% extends 'back/base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Basket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ basket.id }}</td>
            </tr>
            <tr>
                <th>DateCreated</th>
                <td>{{ basket.dateCreated ? basket.dateCreated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>BillingDate</th>
                <td>{{ basket.billingDate ? basket.billingDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_basket_index') }}">back to list</a>

    <a href="{{ path('app_basket_edit', {'id': basket.id}) }}">edit</a>

    {{ include('back/basket/_delete_form.html.twig') }}
{% endblock %}
