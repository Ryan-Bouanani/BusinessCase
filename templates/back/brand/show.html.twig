<!DOCTYPE html>

<title>Brand</title>

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ brand.label }}</td>
            </tr>
            <tr>
                <th>PathImage</th>
                <td>{{ brand.pathImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_brand_index') }}">back to list</a>

    <a href="{{ path('app_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
